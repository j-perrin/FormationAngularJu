import { ColorDayDirective } from './color-day.directive';

describe('ColorDayDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDayDirective();
    expect(directive).toBeTruthy();
  });
});
